#include<bits/stdc++.h>
using namespace std;

void prog(){
    int n; cin >> n;
    int cap; cin >> cap;
    vector<int> val(n), wt(n);
    for(int &x: val) cin >> x;
    for(int &x: wt) cin >> x;

    
}

int main(){
    int t; cin >> t;
    while(t--){
        prog();
    }
}